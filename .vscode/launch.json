{ 
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "launch bot",
            "skipFiles": ["<assets>", "<other>"],
            "program": "${workspaceFolder}/src/main/help.js/"
        }
    ]
}